<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:text="Complete a simple breathing exercise to relax and reduce stress."
        android:gravity="center"
        android:textSize="20dp"
        android:textStyle="bold|italic" />

    <Button
        android:id="@+id/startButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/stopButton"
        android:layout_centerHorizontal="true"
        android:background="@color/colorTeal"
        android:drawableLeft="@drawable/baseline_play_arrow_white_18dp"
        android:paddingLeft="@dimen/fab_margin"
        android:text="Start"
        android:textColor="@android:color/primary_text_dark" />

    <Button
        android:id="@+id/stopButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/restartButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:background="@color/colorBlue"
        android:drawableLeft="@drawable/baseline_stop_white_18dp"
        android:paddingLeft="@dimen/fab_margin"
        android:text="Stop"
        android:textColor="@android:color/primary_text_dark" />

    <Button
        android:id="@+id/restartButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="50dp"
        android:layout_marginTop="10dp"
        android:background="@color/materialOrange"
        android:drawableLeft="@drawable/baseline_replay_white_18dp"
        android:paddingLeft="@dimen/fab_margin"
        android:text="Restart"
        android:textColor="@android:color/primary_text_dark" />


    <Chronometer
        android:id="@+id/simpleChronometer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="115dp"
        android:gravity="center"
        android:padding="10dp"
        android:textColor="@color/colorDarkGray"
        android:textStyle="bold" />

</RelativeLayout>